# Pinned to versions used in local env (conda 'ritmo'). Tested on
# Python 3.10.19. Reproduces the exact pipeline behaviour on AWS EC2.
numpy==1.26.4
pandas==2.3.3
scipy==1.15.2
scikit-learn==1.7.2
statsmodels==0.14.5
matplotlib==3.10.8
mplsoccer==1.6.1
pyarrow==23.0.0
Pillow==12.0.0

# Test runner (only needed for the test suite; safe to install in prod too)
pytest==8.4.2

# Executive-summary slides (only needed for deliverable/make_exec_summary.py)
python-pptx==1.0.2
